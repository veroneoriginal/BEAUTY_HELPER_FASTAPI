[pytest]
asyncio_mode = auto
testpaths = tests
# Корень проекта в sys.path, чтобы import apps/core работал при запуске
# голым `pytest` (в CI), а не только через `python -m pytest`.
pythonpath = .
markers =
    integration: тесты на реальной БД (нужен запущенный Postgres)
